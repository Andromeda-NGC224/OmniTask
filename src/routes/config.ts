export enum EAppRoutes {
  PREVIEW = '/',
  TASKS = '/tasks',
  TEST_PAGE = '/test',
  DASHBOARD = '/dashboard',
  KANBAN = '/kanban',
  PROFILE = '/profile',
  LOGIN = '/login',
  REGISTER = '/register',
}
