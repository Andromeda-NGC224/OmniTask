export enum EAppRoutes {
  PREVIEW = '/',
  TASKS = '/tasks',
  TASKS_DETAILS = '/tasks/:id',
  TEST_PAGE = '/test',
  DASHBOARD = '/dashboard',
  KANBAN = '/kanban',
  PROFILE = '/profile',
  LOGIN = '/login',
  REGISTER = '/register',
}
